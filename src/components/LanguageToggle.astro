---
import { useTranslations, getLanguageAlternate } from '../i18n/utils';

export interface Props {
  lang: 'pt' | 'en';
}

const { lang } = Astro.props;
const t = useTranslations(lang);
const alternateLang = getLanguageAlternate(lang);
const alternateUrl = lang === 'pt' ? '/en' : '/';
---

<a
  href={alternateUrl}
  class="px-3 py-2 rounded-md text-sm font-medium text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400 hover:bg-gray-100 dark:hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-all duration-200 border border-gray-300 dark:border-gray-600"
  title={`Switch to ${alternateLang === 'pt' ? 'Português' : 'English'}`}
>
  {alternateLang === 'pt' ? '🇧🇷 PT' : '🇺🇸 EN'}
</a>